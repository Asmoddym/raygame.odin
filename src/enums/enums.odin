package enums

// Player direction
Direction :: enum {
  NONE,
  LEFT,
  RIGHT,
  UP,
  DOWN,
  DIRECTIONS,
}

// Scene IDs
SceneID :: enum {
  MAIN,
  PAUSE,
}

// Overlay IDs
OverlayID :: enum {
  INVENTORY,
}
